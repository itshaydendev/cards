name: Go

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go
        with:
          go-version: '^1.14.2'
      - uses: actions/checkout
      - run: make
